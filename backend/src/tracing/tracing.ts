// src/tracing/tracing.ts
// Simplified tracing that doesn't cause errors

export function setupTracing() {
  // Tracing is disabled due to version conflicts
  // To enable, fix OpenTelemetry version compatibility
  console.log('Tracing is disabled');
  return null;
}